export const environment = {
  production: true,
  googleMapsAPIKey: 'ADD -- google maps API Key',
  firebaseAPIKey: 'ADD -- firebase APIKey',
    //just for another methods -----------------------------
    config: {
      // add firebase Config
    }
};
